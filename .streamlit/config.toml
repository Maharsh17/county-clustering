# Matches the palette the figures use, so the hosted app and the write-up
# look like the same project.
[theme]
primaryColor = "#2a78d6"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f4f4f2"
textColor = "#0b0b0b"

[server]
headless = true

[browser]
gatherUsageStats = false
