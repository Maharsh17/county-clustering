# Runtime dependencies for the deployed explorer (app/app.py).
#
# Streamlit Community Cloud reinstalls this file from scratch every time a
# sleeping container is rebuilt, so anything only the pipeline needs lives in
# requirements-train.txt instead. scipy is not listed because scikit-learn
# already requires it.
numpy==2.5.1
pandas==3.0.3
plotly==6.9.0
scikit-learn==1.9.0
streamlit==1.60.0
pyyaml==6.0.3
