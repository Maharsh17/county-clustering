# Every dependency, for both app/app.py and scripts/train.py.
#
# Streamlit Community Cloud reinstalls this from scratch each time a sleeping
# container is rebuilt, so it also pulls the three training-only packages:
# matplotlib draws seven of the eight figures, scipy supplies Ward linkage and
# the dendrogram, and kaleido rasterizes the county map to PNG. That costs a
# slower cold start; split the file again if the rebuild gets painful.
numpy==2.5.1
pandas==3.0.3
plotly==6.9.0
scikit-learn==1.9.0
streamlit==1.60.0
pyyaml==6.0.3
matplotlib==3.11.0
scipy==1.18.0
kaleido==1.3.0
